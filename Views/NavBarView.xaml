<UserControl x:Class="FrameDataApp.Views.NavBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FrameDataApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Sleek Rounded Button Style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#9898A6"/>
            <Setter Property="Background" Value="#18181C"/>
            <Setter Property="BorderBrush" Value="#2A2A32"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="18"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover state: Smooth glow + brighter text -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25252E"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#4F4F5E"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <!-- Pressed state -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#121215"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#3F3F4C"/>
                                <Setter Property="Foreground" Value="#D0D0D8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel>
        <!-- Main Top Bar Content Shell -->
        <Border Background="#0F0F12" 
                Padding="24,12">

            <Grid>
                <!-- Modern Minimalist Title -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="8" Height="8" CornerRadius="4" Background="#6366F1" Margin="0,0,10,0"/>
                    <TextBlock Text="FRAME DATA HUB" 
                               Foreground="#EEEEF2" 
                               FontSize="14" 
                               FontWeight="Bold"/>
                </StackPanel>

                <!-- Sleek Rounded Navigation Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Dashboard" Command="{Binding NavigateHomeCommand}" Style="{StaticResource NavButtonStyle}" Margin="4,0"/>
                    <Button Content="Games" Command="{Binding NavigateGameList}" Style="{StaticResource NavButtonStyle}" Margin="4,0"/>
                    <Button Content="Characters" Command="{Binding NavigateCharacterList}" Style="{StaticResource NavButtonStyle}" Margin="4,0"/>
                    <Button Content="Moves" Command="{Binding NavigateMoveList}" Style="{StaticResource NavButtonStyle}" Margin="4,0"/>
                </StackPanel>
            </Grid>

        </Border>

        <!-- Accent Blue Separator Bar -->
        <Border Height="2" Background="#6366F1"/>
    </StackPanel>
</UserControl>